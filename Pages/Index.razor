@page "/"
    
@using BLL

    <AuthorizeView>
    <Authorized>
         <h1>
              Sistema de Ventas <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" fill="currentColor" class="bi bi-cart4" viewBox="0 0 16 16">
  <path d="M0 2.5A.5.5 0 0 1 .5 2H2a.5.5 0 0 1 .485.379L2.89 4H14.5a.5.5 0 0 1 .485.621l-1.5 6A.5.5 0 0 1 13 11H4a.5.5 0 0 1-.485-.379L1.61 3H.5a.5.5 0 0 1-.5-.5zM3.14 5l.5 2H5V5H3.14zM6 5v2h2V5H6zm3 0v2h2V5H9zm3 0v2h1.36l.5-2H12zm1.11 3H12v2h.61l.5-2zM11 8H9v2h2V8zM8 8H6v2h2V8zM5 8H3.89l.5 2H5V8zm0 5a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm-2 1a2 2 0 1 1 4 0 2 2 0 0 1-4 0zm9-1a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm-2 1a2 2 0 1 1 4 0 2 2 0 0 1-4 0z"/>
</svg>
         </h1>

        <div>
        <p>Bienvenido 
           @(context.User.Identity?.Name)</p>   
        </div>

        <div> <p> Sistema de ventas, donde puedes registrar Suplidores, Clientes, Articulos, realizar ventas con detalle y tener control de inventario de tus Articulos. </p></div> 
        

         <div class="container">
        <partial name="_CookieConsentPartial" optional />
        <main role="main" class="pb-1">
           
        </main>
    </div>
    <footer class="footer border-top pl-3 text-muted">
        <div class="container">
        &copy; Developers Jr
            
        </div>
    </footer>


    </Authorized>
    <NotAuthorized>
        <h1>Sistema de Ventas</h1>

        Debe <a href="Identity/Account/Login">Iniciar sesión</a>, o <a href="Identity/Account/Register">Registrarse</a> para utilizar el sistema de registros.

    </NotAuthorized>
</AuthorizeView>

@code
{
    #nullable disable // Para quitar el aviso de nulls

    [Inject]
    public UsuariosBLL usuarios { get; set; }

    List<Usuarios> Lista = new List<Usuarios>();

    protected override void OnInitialized()
    {
        Lista = new List<Usuarios>();
    }

    public async Task Consultar()
    {
        Lista = await usuarios.GetList(c => true);
    }
}